# ct-translate ガードレール（翻訳の絶対ルール）

> このフォルダで翻訳するとき必ず守る。Skill の手順より優先。

1. **義務・禁止・警告表現を弱めない／省略しない** — 「しなければならない」→ *must*、「禁忌」→ *contraindication*。*should* への弱化や脱落は不可。
2. **数値・単位・略号・固有名は保存** — 用量・期間（例: 24時間）・p 値・略号（SAE / ICF 等）を改変しない。
3. **用語は glossary P0 を最優先** — 顧客指定訳に従い、禁止訳を使わない。
4. **不明な専門語を創作しない** — 確証がなければ訳を当てず `⚠️要確認` を付けて人間に回す。
5. **DRAFT と CONFIRMED を区別** — 人手承認（HITL）を経るまで CONFIRMED にしない。
6. **出典コメントを必ず残す** — どの原文のどこを訳したか追跡可能に（`<!-- src: file L行 -->`）。
7. **秘密情報を外部に出さない** — `sources/` の内容を無断で外部サービスに送らない。**API キーをこのフォルダに置かない**（環境変数で渡す）。
8. **モデルは固定（pin）し、変更は変更管理（リバリデーション）対象** — アカウント・ホスト・API 経由は自由に変えてよいが、**モデル（とそのバージョン）を変えると出力品質が変わる**。本番では使用モデルを明記し、変更時は `experiments/run-log.md` の 4 段ラダーで再評価する（Part 11 リバリデーション）。期待値は「同じ訳文が毎回出る」ではなく「品質が毎回担保される」。
