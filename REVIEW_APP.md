# レビューアプリとは — ハーネスの出力を CAT ツールの型で裁く

## 一言でいうと

レビューアプリとは、**翻訳ハーネスが出した訳を、人が1文ずつ確認して確定する作業台**のこと。翻訳プロセスにおける **HITL（Human-in-the-Loop）判断ノードそのもの**であって、後付けの QA 工程ではない。

その中身は、翻訳業界で30年枯れた **CAT（Computer-Assisted Translation）ツール**の型をそのまま継承すればよい（再発明しない）。**現代化は1点だけ** —— CAT の「MT サジェスト欄」を、**このハーネスの出力＋その自己申告**（用語ヒット・逆翻訳投票・確信度）に置き換える。これで「全セグメントを平等に見る」をやめ、**機械が採点し並べる → 人は要注意なものだけ裁く**構造にできる（人手の工数がセグメント数でなくフラグ数に比例する）。

> ⚠️ CAT ツール（＝翻訳を**裁く/管理する**環境。Trados・memoQ・Phrase 等）と、MT エンジン（＝翻訳を**作る**もの。DeepL・各種 NMT 等）は別カテゴリ。レビューアプリは前者にあたる。MT エンジンは後者で、レビューアプリの「サジェスト欄」に下訳を供給する側。

## CAT ツールの代表機能と、このハーネスでの対応物

このリポジトリ（`ct-translate`）は、実はレビューアプリの構成要素をファイルとして既に持っている。

| CAT 機能 | 中身 | `ct-translate` での対応物 |
|---|---|---|
| セグメンテーション | 原文を文単位に分割し原文↔訳文を1:1整列 | 翻訳時の文分割 |
| 対訳エディタ | 左=原文／右=訳文の2ペインで1行ずつ見る | （画面を作るならここが中核） |
| 翻訳メモリ（TM） | **承認済み**対訳の再利用DB | `memory.md`（確定対訳の追記＝フライホイール） |
| 用語ベース（termbase） | 承認用語集。必須語・禁止語をフラグ | `glossary/*.md`（P0→P3 先勝ち） |
| QA チェック（自動） | 用語・数値・タグ・未訳・一貫性の機械検査 | SKILL のガードレール（義務表現・数値・禁忌） |
| MT 統合 | MT サジェストを TM と同じ欄に出す | ★**このハーネスの訳**がその欄に来る |
| Quality Estimation（QE） | 訳質を推定スコア化し要修正を優先表示 | ★逆翻訳投票（DW）の一致度が素材になる |
| LQA / MQM | 誤りをカテゴリ×重大度で構造記録 | 差戻し理由の構造化（下記） |
| コンコーダンス検索 | 「前はどう訳した?」を過去訳から全文検索 | `memory.md` の grep |
| プロパゲーション | 同一文の訳を文書内で自動反映 | 反復文の一括処理 |
| インコンテキスト プレビュー | 完成レイアウトで確認 | （体裁が要る文書のみ・後回し可） |
| コメント/版管理/監査証跡 | 誰がいつ何を、履歴、署名 | git のコミット履歴＋出典コメント |

## パイプライン → レビュー画面の受け渡し（review package）

レビュー画面を作るなら、ハーネスは訳文だけでなく1セグメントあたり次を渡すとよい。これがあると triage（並べ替え）と「なぜこの訳か」の説明ができる。

```
source_text     原文
target_draft    ハーネス訳
glossary_hits[] {原語, 期待訳, 適用されたか}    ← 用語逸脱の検出
flags[]         {種類, 重大度, メッセージ, 止める/注意}
qe_score        0-100（逆翻訳の一致度などから合成した確信度）
backtranslation 逆翻訳文＋投票結果（「なぜこの訳か」の腹落ち材料）
provenance      {使用モデル, バージョン(pin), ts}   ← 「同一出力でなく品質担保」の根拠
status          未裁定 / auto-pass / 要確認
```

## Triage — 機械が並べ、人は要注意だけ裁く

優先度キュー：`止めるフラグ有` ＞ `qe_score 低位` ＞ `注意フラグのみ` ＞ `auto-pass`。

- **auto-pass**：全 QA 通過 ∧ フラグ無 ∧ qe_score が閾値以上のセグメントは、デフォルト accept 候補。人はスキップしてよい（ただし「auto-pass を承認した」ことは記録に残す）。
- **止める/注意の使い分け**：用語・規制のように外せない軸は「止める（赤）」、文体・体裁のように助言でよい軸は「注意（黄）」。この色分けがそのまま並び順を決める。

## LQA / MQM とは（差戻しの型）

差戻し（reject）のとき「なんとなく変」ではなく、決まった分類箱に入れて記録すると、後で数えられる・比べられる・改善に回せる。

- **LQA**（Linguistic Quality Assurance）＝訳を「決めた誤り分類」に照らして客観採点する**プロセス全体**。
- **MQM**（Multidimensional Quality Metrics）＝その採点に使う**誤りの分類表**。カテゴリ（Accuracy/Terminology/Fluency/Style…）× 重大度（critical/major/minor/neutral）の2軸。
- **DQF-MQM**＝翻訳業界団体 TAUS の運用枠組み（DQF）＋ MQM の統合。de facto 標準。

構造化した差戻し理由は、ハーネスの用語集・ガードレール・検証を賢くする**唯一の燃料**になる（＝レビューがフライホイールの入口）。ただし用語集への昇格は**承認ゲート必須**（無断昇格は訳ブレの原因になる）。

## アクションと記録

- **accept**（そのまま確定・一括可）／**edit**（**変更理由を必須**で残す）／**reject**（MQM で分類）／**文書サインオフ**（誰が確定したかを記録）。
- 記録は**追記のみ**（上書きしない）。「誰が・いつ・何を・なぜ」が残ることが、非決定論な AI 出力を「人が確認した」形で統制内に取り込む担保になる（期待値は "同じ訳文" でなく "品質が毎回担保される"、[README.md](README.md) の「移植と精度の鉄則」と同じ思想）。

## 関連

- [README.md](README.md) — 本リポジトリ全体の使い方・4段ラダー実験
- [GROUNDING.md](GROUNDING.md) — 用語集（termbase）を含むグラウンディング5方式の解説
- `memory.md` — 翻訳メモリ（TM）の実体／`glossary/` — 用語ベース（termbase）の実体
