# グラウンディングとは — このリポジトリが「用語集MD」を選んだ理由

## 一言でいうと

グラウンディングとは、**AIに「記憶だけに頼らず、手元の資料を見ながら答えさせる」仕組み**のこと。

- **グラウンディングなし** = 教科書を閉じて記憶だけで答える試験。細かい用語や社内独自のルールは"うろ覚え"になりがちで、間違えたり適当に埋めたりする（＝ハルシネーション）。
- **グラウンディングあり** = 「この資料を見ながら答えていい」という持ち込み可の試験。AIは答えを作る前に渡された資料（用語集・過去文書等）を参照し、その内容に沿って回答する。

このリポジトリの `glossary/*.md` は、まさにこの「持ち込み資料」にあたる。

## 5つの渡し方 — 性質もコストも別物

グラウンディングは一つの仕組みではなく、実際には「参照資料をどう持たせるか」で複数の方式があり、性質もコストも別物になる。

| 方式 | 仕組み | アナロジー | コストの性質 |
|---|---|---|---|
| **①TSV/MD用語集**（本リポジトリの方式） | 原語→訳語1対1の対訳表をファイルで渡す。決定論的（同じ入力は必ず同じ訳語） | 単語帳を渡す | ファイル代のみ＝$0、差し替え自由 |
| ②Dynamic dictionary | ファイル登録せず、リクエストの中に都度「ここはこう訳して」と埋め込む | 付箋を貼る | ほぼ$0 |
| ③Custom Translator（フル学習） | 対訳ペアでモデル自体を再教育。**10,000ペア以上**が必要（文レベルでアラインメントされた対訳の本数） | 通訳者に1年間の専門研修を受けさせる | 学習のたびに新規文字数課金＋翻訳単価も割高 |
| ④AdaptCT/Adaptive custom translation | RAG+Few-shot。5ペアから使え、既存モデルにそのまま乗る（再教育なし） | カンニングペーパーを都度渡す | 低コスト、更新はインデックス再構築のみ |
| ⑤AI Search RAG系 | 文書全体を検索可能な索引にし、関連断片を都度参照させる | 専属の司書を24時間常駐させる | 24/7固定課金、待機している間ずっと発生（停止＝削除のみ、一時停止不可な構成が多い） |

## なぜ①（MD用語集）を選んだか

- **決定論的**: `grep` でのファイル順先勝ち（P0→P1→P2→P3）は、同じ入力に対して常に同じ結果を返す。③④⑤のようなモデル依存・検索依存の揺れがない。
- **コストゼロ**: ファイルを置くだけ。③のような学習課金、⑤のような待機課金が発生しない。
- **監査可能**: 「なぜこの訳語になったか」がファイルの中身をそのまま指せる。③（モデル重みに溶け込む）に比べて追跡性が高い。
- **育てられる**: `memory.md → p2-house.md` への昇格フライホイールで、運用しながら太っていく。①は「静的な単語帳」で終わらせず、③④のような学習コストなしに継続改善できる。

③④⑤が有効になるのは、①では対応しきれない要件が出たとき:
- **③フル学習**への昇格は、対訳ペアが**10,000組**に届くかどうかが分岐点（届かなければ dictionary-only が現実解）
- **⑤AI Search**が要るのは、単語ではなく「版のある文書全体」（例：ICH/PMDA等の規制ガイドライン）を検索・参照させる必要が出たとき。判断軸は文書量ではなく「版管理を誤ったリスク・手間」と「常時稼働コスト」の天秤

## 関連

- [README.md](README.md) — 本リポジトリ全体の使い方・4段ラダー実験
- `.claude/skills/translate/SKILL.md` — ①を組み込んだ翻訳ハーネス本体
- `glossary/` — ①の実装そのもの
